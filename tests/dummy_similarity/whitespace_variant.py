"""
Whitespace-only variant

Purpose:
- Functionally identical to exact_* samples but with spacing differences.
- Should still be clustered by the exact strategy (which collapses whitespace).
"""


def util_sum(a, b):
    """Return the sum of two values (dummy)."""
    tmp = a + b
    return tmp
