"""
Exact duplicate sample (A)

Purpose:
- This function is intentionally duplicated in multiple files to be detected
  by the exact similarity strategy (whitespace-insensitive hash).
"""


def util_sum(a, b):
    """Return the sum of two values (dummy)."""
    tmp = a + b
    return tmp

